import { TaskCompletedEventObject } from '../../avs/contracts/ReclaimServiceManager';
import { CreateClaimOnAvsOpts } from '../../avs/types';
import { ProviderClaimData } from '../../proto/api';
import { ProviderName } from '../../types';
/**
 * Creates a Reclaim claim on the AVS chain.
 */
export declare function createClaimOnAvs<N extends ProviderName>({ onStep, createClaimOnAttestor, chainId, payer, ...opts }: CreateClaimOnAvsOpts<N>): Promise<{
    claimData: ProviderClaimData;
    object: TaskCompletedEventObject;
    txHash: string;
}>;
