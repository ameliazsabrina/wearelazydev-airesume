"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.globalGnarkLib = void 0;
exports.makeGnarkZkOperator = makeGnarkZkOperator;
const js_base64_1 = require("js-base64");
const config_1 = require("../config");
const utils_1 = require("./utils");
function makeGnarkZkOperator({ algorithm, fetcher }) {
    let initDone = false;
    return {
        async generateWitness(input) {
            return (0, utils_1.generateGnarkWitness)(algorithm, input);
        },
        async groth16Prove(witness, logger) {
            const lib = await initGnark(logger);
            const { prove, koffi, free } = lib;
            const wtns = {
                data: Buffer.from(witness),
                len: witness.length,
                cap: witness.length
            };
            const res = prove(wtns);
            const proofJson = Buffer.from(koffi.decode(res.r0, 'unsigned char', res.r1)).toString();
            free(res.r0); // Avoid memory leak!
            return JSON.parse(proofJson);
        },
        async groth16Verify(publicSignals, proof, logger) {
            const lib = await initGnark(logger);
            const { bitsToUint8Array } = config_1.CONFIG[algorithm];
            const proofStr = proof['proofJson'];
            const verifyParams = {
                cipher: algorithm,
                proof: proofStr,
                publicSignals: js_base64_1.Base64.fromUint8Array(bitsToUint8Array(publicSignals.flat())),
            };
            const paramsJson = JSON.stringify(verifyParams);
            const paramsBuf = (0, utils_1.strToUint8Array)(paramsJson);
            const params = {
                data: paramsBuf,
                len: paramsJson.length,
                cap: paramsJson.length
            };
            return lib.verify(params) === 1;
        },
    };
    async function initGnark(logger) {
        exports.globalGnarkLib || (exports.globalGnarkLib = (0, utils_1.loadGnarkLib)());
        const lib = await exports.globalGnarkLib;
        if (initDone) {
            return lib;
        }
        const { id, ext } = utils_1.ALGS_MAP[algorithm];
        const [pk, r1cs] = await Promise.all([
            fetcher.fetch('gnark', `pk.${ext}`, logger),
            fetcher.fetch('gnark', `r1cs.${ext}`, logger),
        ]);
        const f1 = { data: pk, len: pk.length, cap: pk.length };
        const f2 = { data: r1cs, len: r1cs.length, cap: r1cs.length };
        await lib.initAlgorithm(id, f1, f2);
        initDone = true;
        return lib;
    }
}
